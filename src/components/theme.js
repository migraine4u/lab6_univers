import { createTheme } from '@mui/material/styles';

let theme = createTheme({
    palette: {
        primary: {
            main: '#97d6c3',
        },
        secondary: {
            main: '#92ccbb',
        },
    },
    spacing: 4,
});

export default theme;
